@model IEnumerable<ProjetArchi.Models.Clients>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.admin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Personnes.Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.admin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Personnes.Nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_client }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_client }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_client })
        </td>
    </tr>
}

</table>
